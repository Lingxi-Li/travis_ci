language: cpp
sudo: false
dist: trusty

addons:
  apt:
    sources:
    - ubuntu-toolchain-r-test
    - llvm-toolchain-trusty-6.0
    packages:
    - clang++-6.0
    - g++-7.2

script:
  - clang++-6.0 --version
  - g++-7.2 --version
